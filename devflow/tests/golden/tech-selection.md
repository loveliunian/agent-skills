# 设计决策记录 - 演示支付

> 选型日期：2026-09-16　选型人：张三　评审人：架构师-老周


<!-- 审计指纹: tech-selection.json sha256=4b03bd3d7a240212a4c55e6e62a72355c62ebd6955cc605d3972b15f77750d40（由 df_render 自动生成，人工勿改） -->

## Step 1 现有仓库与硬约束盘点

| 项 | 内容 |
|---|---|
| 现有技术栈 | Spring Boot 3.2 / PostgreSQL 15 / Redis 7（读取自 backend/pom.xml 与 deploy/） |
| 技术约束契约 | docs/需求/demo-pay-技术约束.md |

## Step 2 硬约束淘汰

| constraint_id | 候选方案 | 判定 | 淘汰理由 |
|---|---|---|---|
| TC-TECH-001 | FlowCore | 淘汰 | 违反 TC-TECH-001 MUST_NOT_USE：禁用产品 FlowCore 出现在候选中 |

## Step 3 缺失事实补充

| 缺失事实 | 获取方式 | 结论 |
|---|---|---|
| Camunda 7.24 与 Spring Boot 3.2 兼容性 | POC 启动验证 | 启动正常，流程部署与执行通过 |

## 候选方案（经 Step 2 淘汰后剩余）

### 方案 A：Camunda 7.24.0

成熟开源工作流引擎，与现有 Java 栈集成成本低

优点：社区成熟；团队有使用经验

缺点： license 版本限制 

### 方案 B：自研轻量状态机

仅覆盖支付单状态流转的极简状态机

优点：零外部依赖

缺点：缺少审批/回滚等编排能力；自研维护成本高

## 决策矩阵（评分只对 Step 2 后剩余方案；分数必须有证据）

| 维度 | 权重 | 方案A Camunda 7.24.0 评分(1-5)/证据 | 方案B 自研轻量状态机 评分(1-5)/证据 |
|---|---|---|---|
| 集成兼容性 | 40% | 5（POC 启动验证通过（facts[0]）） | 3（需自行适配事务边界，引用内部评审记录） |
| 功能覆盖 | 35% | 5（官方文档：审批/回滚/定时边界事件齐全） | 2（需求盘点：缺 3 项编排能力） |
| 维护成本 | 25% | 4（团队 2 人有生产经验（技能矩阵）） | 2（自研需长期 1 人维护（估算）） |
| **加权合计** | 100% | 475 | 240 |

> 权重按本项目实际关注点确定；无证据的评分无效。

## 决策结论

选定方案 **A：Camunda 7.24.0**（加权合计 475）

用户确认: 已确认（Gate 机检行）

### 决策理由

```
加权合计 A=470 领先；集成兼容性与功能覆盖证据充分（POC + 官方文档），维护成本可接受
```

### 风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| Camunda 社区版 license 变更 | 中 | 锁定 7.24.0 并跟踪 release note |

## 详设文档结构决策

> 文档结构（单文档/总分）在 P1 选型时决策并冻结；P2 详细设计只按本结论选模板，不再重复决策。

design_doc_structure_mode=monolith（P1/P2 Gate 机检行；s2 --mode 与此一致）

| 决策项 | 结论 |
|---|---|
| 文档结构 | monolith（单文档） |
| 决策依据 | 支付能力为单模块内聚增强，无跨服务拆分，1 个团队交付，单文档可完整承载 |

## 脚手架重合度审计

> 铁律 18：输入含脚手架/存量代码时逐功能域二分裁决——重合 → 裁剪并由本次设计重新实现（宜独立新模块承载）；不重合 → 复用既有能力。禁止同一功能域新旧双实现并存；裁剪/复用/新建项由 P2 baseline（DELETE/MODIFY/REUSE/ADD）承接。

| 功能域 | 脚手架现状（代码/菜单/页面/表） | 判定（裁剪/复用/新建） | 处置动作 | 新实现承载位置 |
|---|---|---|---|---|
| F01 支付单管理 | module-scaffold 已有 PayController + pay_order 表 + 菜单 101 + 前端支付页面 | 裁剪 | 删除脚手架 PayController/Service/Entity 与前端页面路由、下线菜单 101、迁移清理引用 | module-pay 重新实现 |
| F02 用户与权限 | 脚手架 common 提供鉴权过滤器与 UserContext | 复用 | REUSE 既有鉴权过滤器与 UserContext，不重复实现 | common-auth（复用） |

## 设计取舍

| ID | 决策点 | 背景 | 选定方案 | 备选（被否决） | 理由 | 关联 |
|---|---|---|---|---|---|---|
| DT-1 | 分页方案 | 列表页数据量增长后 offset 分页性能退化 | cursor 分页（基于 create_time + id） | offset 分页（简单但大偏移量慢） | 预估数据量 > 100 万，offset 分页 P95 超 500ms | §3.2.1 |
| DT-2 | 删除策略 | 物理删除导致审计与恢复需求无法满足 | 逻辑删除（deleted=1 + 版本号递增） | 物理删除 + 回收站表 | 逻辑删除可与乐观锁共用 version 列，减少一张回收站表 | §3.2.5 |

## 规范遵循

<!-- anchor: standards-compliance -->

> v3.28.1 起从详设 §13 移入：命名/开发/注释/数据库等规范域基线；偏离须列明理由（规范依据是 DDR 与 P2a 评审的引用正本）。

| 规范域 | 采用规范 | 版本/链接 | 本设计落点/偏离说明 |
|---|---|---|---|
| 命名规范 | 阿里巴巴 Java 开发手册 | 黄山版 | 详设 §2.2 表名/字段名小写蛇形；Java 类名 UpperCamel |
| 开发规范 | 阿里巴巴 Java 开发手册 | 黄山版 | 分层/异常/日志按手册；偏离见四方言契约说明 |
| 注释规范 | 阿里巴巴 Java 开发手册 + Javadoc | 黄山版 | 公开接口与复杂规则 Javadoc |
| 数据库规范 | 阿里巴巴 Java 开发手册（MySQL 章） | 黄山版 | 方言专有写法以四方言一致性契约为准 |

## 硬约束绑定（机器契约；Gate 按 此块 判定合规）

<!-- DEVFLOW:CONSTRAINT-BINDINGS
constraint_id=TC-TECH-001
selected_product=camunda
selected_version=7.24.0
compliance=PASS
evidence=backend/pom.xml 依赖坐标 org.camunda.bpm
DEVFLOW:END -->

## 实施计划

| 任务 | 负责人 | 完成日期 |
|---|---|---|
| 依赖引入与 POC 复验 | 张三 | 2026-09-18 |
| 集成测试 | 王五 | 2026-09-22 |

## 评审签字

| 角色 | 姓名 | 日期 |
|---|---|---|
| 技术负责人 | 老周 | 2026-09-16 |
| 架构师 | 老周 | 2026-09-16 |
