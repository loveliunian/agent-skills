---
name: documentation
version: "3.31.4"
description: "Use when updating technical documentation including API docs, operational docs, and internal knowledge base."
number-scheme: legacy-P-retained
devflow-phase: P9
s-phase: (无对应)
rationale: "文档更新是独立业务闭环,S 轨不涉及。保留 P 编号。"
---

# Phase 9: 文档更新 (Documentation)

## 完备性硬要求（v3.27.5，M-01 教训 L-M01-009/010）

1. **接口文档独立成文**：禁止以"完整契约见详设 §3.2"引用代替正文。每接口须含：请求/响应 JSON 示例、错误码逐条列举、分页/排序参数规约、curl 调用样例。若服务引入 springdoc，须实际验证 swagger-ui 可访问（防死配置）。
2. **事实源填实**：`docs/详细设计/_环境与账号.md` 等骨架事实源不得残留 `{dialect}`/`{component}` 类占位符（P6 凭证门已固化扫描）。
3. **git 初始化**：交付工作区必须 `git init` + 阶段性提交——无提交历史则 P4b"反查真实文件"退化为存在性检查，且无回滚能力。
4. **auto 索引刷新**：INDEX-接口/表-auto.md 等生成索引在实现完成后必须重跑生成脚本（对齐 P4b 实测）。

---

## 目标

- 保持文档与代码同步
- 方便后续维护
- 知识传承
- 新人 onboarding

---

## 必须更新的文档

| 文档类型 | 更新时机 | 负责人 |
|----------|----------|--------|
| **API文档** | 新增/修改接口 | 后端开发 |
| **数据库文档** | 表结构变更 | 后端开发 |
| **部署文档** | 部署方式变更 | 运维 |
| **架构文档** | 架构变更 | 架构师 |
| **运维手册** | 运维流程变更 | 运维 |
| **用户手册** | 功能变更 | 产品 |

---

## 语言总纲（v3.18.0）

> 本阶段全部文档（API/数据库/部署/运维/用户手册）的中文表达遵循 `concepts/中文文风规范.md`：
> 说人话——主谓宾完整、一句话一个主张、结论自己站着（表格只做佐证）、数字给单位和口径、
> 同一事物全文一个名字、不把工具/校验器内部机制写进面向读者的正文。

## API文档规范

### 接口文档生成（确定性步骤，v3.27.11）

存在 `.devflow/<feature>/design.json` 时，接口文档必须先用生成器产出确定性骨架（每接口请求/响应 JSON 示例、错误码逐条、curl 样例、权限码），再人工补齐业务说明；禁止只写"完整契约见详设 §x"：

```bash
python3 "$SKILL_ROOT/scripts/generate-api-doc.py" \
  .devflow/<feature>/design.json \
  docs/接口文档/<feature>-接口文档.md --feature <feature>
```

### OpenAPI/Swagger格式

```yaml
openapi: 3.0.0
info:
  title: 用户服务API
  version: 1.0.0
paths:
  /api/users/{id}:
    get:
      summary: 获取用户信息
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
```

### 必填字段

| 字段 | 说明 |
|------|------|
| 接口名称 | 清晰的中文描述 |
| 请求方法 | GET/POST/PUT/DELETE |
| 请求路径 | API路径 |
| 请求参数 | 参数名、类型、必填、说明 |
| 请求示例 | JSON格式示例 |
| 响应参数 | 字段说明、类型 |
| 响应示例 | 成功/失败示例 |
| 错误码 | 可能的错误及处理方式 |

---

## 数据库文档规范

### 表结构文档

```markdown
# 表名: user

## 说明
用户信息表，存储用户基本信息

## 字段说明

| 字段名 | 类型 | 长度 | 约束 | 说明 |
|--------|------|------|------|------|
| id | bigint | 20 | PK, AUTO_INCREMENT | 主键 |
| username | varchar | 50 | NOT NULL, UNIQUE | 用户名 |
| email | varchar | 100 | NOT NULL | 邮箱 |
| created_at | datetime | - | NOT NULL | 创建时间 |

## 索引

| 索引名 | 字段 | 类型 |
|--------|------|------|
| idx_username | username | 普通索引 |
| idx_email | email | 唯一索引 |

## 变更历史

| 日期 | 变更内容 | 负责人 |
|------|----------|--------|
| 2026-01-01 | 初始创建 | xxx |
```

---

## 部署文档规范

### 部署手册

```markdown
# [服务名称] 部署文档

## 环境信息
- 环境: 生产环境
- 机器: xxx.xxx.xxx.xxx
- 端口: 8080

## 部署方式
[Docker/K8s/手动]

## 部署步骤

### 1. 环境准备
```bash
# 需要的依赖
```

### 2. 配置
```bash
# 配置文件位置
```

### 3. 部署
```bash
# 部署命令
```

### 4. 验证
```bash
# 验证命令
```

## 回滚步骤
[回滚命令和步骤]

## 常见问题
| 问题 | 解决方案 |
|------|----------|
|      |          |
```

---

## 文档验收Checklist

### API文档
- [ ] 新增接口已文档化
- [ ] 修改接口已更新
- [ ] 请求/响应示例完整
- [ ] 错误码说明完整

### 数据库文档
- [ ] 新增表已文档化
- [ ] 字段变更已更新
- [ ] 索引变更已记录

### 部署文档
- [ ] 部署步骤完整
- [ ] 配置说明清楚
- [ ] 回滚方案明确
- [ ] 常见问题有解答

### 其他文档
- [ ] README已更新
- [ ] 架构图已更新
- [ ] 依赖关系已更新

---

## 文档存放位置

```
docs/
├── api/                    # API文档
│   ├── user.md
│   └── order.md
├── database/               # 数据库文档
│   └── schema.md
├── deployment/             # 部署文档
│   └── production.md
├── architecture/           # 架构文档
│   └── overview.md
└── guides/                 # 使用指南
    └── user-guide.md
```

---

## 验收Checklist

- [ ] API文档已更新
- [ ] 数据库文档已更新
- [ ] 部署文档已更新
- [ ] 相关文档已提交
- [ ] 文档可读性检查通过

## Gate 检查

| 检查项 | 通过条件 |
|--------|----------|
| 文档数量 ≥ 5 | `find docs/ -name '*.md' -not -path '*/node_modules/*' | wc -l >= 5` |
| API 文档同步 | API 数量与设计一致 |
| 数据库文档同步 | 表数量与设计一致 |

---

## 结构化产物层（v3.25.2）

本阶段产物已结构化：AI 按 `schemas/docs-index.schema.json`（样例 `examples/structured/docs-index.sample.json`）填 `.devflow/<feature>/docs-index.json`，再跑管线校验并确定性渲染 Markdown——**校验失败不渲染、不落盘、不进 Gate**；空集合必须 `zero_results` 显式声明。渲染格式与阶段 Gate 的机器解析契约逐字段兼容，模板保留为语义参考。

```bash
python3 scripts/df_pipeline.py docs-index --input .devflow/<feature>/docs-index.json --out docs/<feature>-文档索引.md
```
