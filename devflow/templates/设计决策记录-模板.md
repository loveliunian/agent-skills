---
name: 设计决策记录-模板
version: "3.29.6"
description: 设计决策记录模板（P1 阶段；先硬约束淘汰，再对剩余方案评分；不预填分数）
---

> **v3.25.2 结构化产物**：本产物已 JSON 化——按 `schemas/tech-selection.schema.json` 填 `.devflow/<feature>/tech-selection.json`，经 `python3 scripts/df_pipeline.py tech-selection` 校验后确定性生成本文档（样例见 `examples/structured/tech-selection.sample.json`）；校验失败不渲染、不进 Gate。本模板保留为语义参考。

# 设计决策记录模板 - {FeatureName}

> 选型顺序是硬性的：**先读现有仓库与硬约束 → 淘汰不合规方案 → 补充缺失事实 → 只对剩余方案评分 → 输出决策**。
> 评分表的分数必须来自实测/引用证据，禁止预填示例分、禁止用 ✓/⭐ 代替分数、禁止把示例当结论。

## 基本信息

| 项 | 内容 |
|---|------|
| 功能名称 | {FeatureName} |
| 选型日期 | {YYYY-MM-DD} |
| 选型人 | |
| 评审人 | |

## Step 1 现有仓库与硬约束盘点

| 项 | 内容 |
|---|------|
| 现有技术栈 | {从仓库实际依赖读取：framework 版本/中间件/部署环境} |
| 技术约束契约 | `docs/需求/{feature}-技术约束.md`（P0 FROZEN） |
| 硬约束清单 | {逐条列出 constraint_id + MUST_USE/MUST_NOT_USE；无约束写 NONE} |

## Step 2 硬约束淘汰

| constraint_id | 候选方案 | 判定 | 淘汰理由 |
|---|---|---|---|
| {TC-XXX-000} | {候选} | 淘汰/保留 | {违反 MUST_USE/MUST_NOT_USE 的具体点} |

> 与硬约束冲突的方案在此出局，不进入 Step 4 评分——加权评分不能覆盖硬约束。

## Step 3 缺失事实补充

| 缺失事实 | 获取方式（POC/基准测试/官方文档） | 结论 |
|---|---|---|
| {事实} | {怎么测/查} | {实测结果} |

## 候选方案（经 Step 2 淘汰后剩余）

### 方案 A：{方案A名称}

**描述：**
```
{方案A的简要描述}
```

**优点：**
- 优点1
- 优点2

**缺点：**
- 缺点1
- 缺点2

### 方案 B：{方案B名称}

**描述：**
```
{方案B的简要描述}
```

**优点：**
- 优点1
- 优点2

**缺点：**
- 缺点1
- 缺点2

> **Gate 标准**：决策矩阵必须 ≥ 3 个评估维度，且至少评估 2 个候选方案。

## 决策矩阵（评分只对 Step 2 后剩余方案；分数必须有证据）

| 维度 | 权重 | 方案A 评分(1-5) | 方案A 证据 | 方案B 评分(1-5) | 方案B 证据 |
|------|------|----------------|-----------|----------------|-----------|
| {维度1，如性能} | {N}% | | {基准/引用} | | {基准/引用} |
| {维度2，如稳定性} | {N}% | | {基准/引用} | | {基准/引用} |
| {维度3，如集成兼容性} | {N}% | | {基准/引用} | | {基准/引用} |
| **加权合计** | 100% | | — | | — |

> 权重按本项目实际关注点确定并说明理由；不使用模板预设权重。无证据的评分无效。

## 决策结论

### 选定方案

**方案 {X}：{方案名称}**

用户确认: （留空；必须由用户明确批准后填写"已确认"或"YES"）

### 决策理由

```
{选择理由，须引用决策矩阵中的证据}
```

## 脚手架重合度审计（铁律 18；输入含脚手架时必填）

> 由 tech-selection.json `scaffold_audit` 渲染生成（非脚手架项目省略本节）；s1 Gate 对账章节与判定清单表存在性。

> 逐功能域对照「本次新功能 vs 脚手架已有能力」；二分裁决不允许模糊态：
> **重合 → 裁剪脚手架对应实现并由本次设计重新实现（宜独立新模块承载）；不重合 → 复用脚手架既有能力**。
> 禁止同一功能域新旧双实现并存；裁剪/复用清单被 P2 baseline（DELETE/MODIFY/REUSE/ADD）承接。

| 功能域 | 脚手架现状（代码/菜单/页面/表） | 判定（裁剪/复用/新建） | 处置动作 | 新实现承载位置 |
|------|------|------|------|------|
| {F0x} | {module-xxx / XxxController / 菜单 10x / 页面 xxx} | {裁剪} | {删除代码/下线菜单/移除页面路由/迁移清理} | {module-<feature> 重新实现} |

## 详设文档结构决策（v3.27.7）

> 由 tech-selection.json 的 `design_doc_structure` 确定性渲染（含机检行，勿手改）。
> 单文档/总分在 P1 决策，P2 详细设计只按此取模板，不再做总分 vs 单体决策。

| 决策项 | 结论 |
|---|---|
| 文档结构 | {monolith 单文档 / total 总分文档} |
| 决策依据 | {模块数量/跨服务依赖/团队并行等事实} |
| 计划文档清单 | {total：总文档 TOTAL + 各分文档 M-NN 路径；monolith：NONE} |

### 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 风险1 | 中 | 应对措施1 |

## 硬约束绑定（机器契约；Gate 按 此块 判定合规）

<!-- DEVFLOW:CONSTRAINT-BINDINGS
constraint_id=TC-TECH-001
selected_product={实际选定产品名}
selected_version={实际版本}
compliance=PASS
evidence={依赖坐标/POC/配置路径}

constraint_id=TC-TECH-002
selected_product={实际选定产品名——禁止写"未引入X"这类否定描述}
compliance=PASS
evidence={依赖扫描或配置证据}
DEVFLOW:END -->

> 无硬约束（constraint_set=NONE）时，本块写一行 `constraint_set=NONE` + `confirmed=true` 即可。
> 规则：`selected_product` 必须写**实际选定的产品名**；`compliance=PASS` 必须有 evidence 支撑。

## 实施计划

| 任务 | 负责人 | 完成日期 |
|------|--------|----------|
| 依赖引入/POC 验证 | | |
| 集成测试 | | |

## 评审签字

| 角色 | 姓名 | 日期 | 意见 |
|------|------|------|------|
| 技术负责人 | | | |
| 架构师 | | | |
| 开发负责人 | | | |
