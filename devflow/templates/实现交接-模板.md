---
name: 实现交接-模板
version: "3.29.5"
description: >-
  实现交接施工图（现有基线/预计变更/不变量；v3.28.1 起从详设移出，独立成文）。
  /plan、/build 与开发子代理的施工图输入正本；正本为 design.json baseline.entries[]。
---

# {FeatureName} 实现交接（Implementation Handoff）

> 模板 ID：`实现交接-模板`
> 模板版本：`{{template_version}}`（必须替换为本模板 frontmatter 的 version 后再过 Gate）
> 生成时间：{YYYY-MM-DD HH:mm:ss}

<!-- anchor: implementation-handoff -->

> **文档定位（v3.28.1）**：实现交接从详设正文移出，本文件为独立正本；`/plan`、`/build`、
> backend/frontend/sql-dev 子代理的施工图输入。机器契约正本为 design.json `baseline.entries[]`。
>
> 编码 Agent 的施工图：写清"现有代码在哪、改动落哪、什么不能变"。基线来自 P2 的现有代码基线调查
> （见 `phases/02-详细设计.md`）；绿地项目（无既有代码）基线表写"新增"，证据列为计划新增路径。
> **机器契约（v3.24.0/A02）**：本节内容必须与 design.json `baseline.entries[]` 一致——
> REUSE/MODIFY/DELETE 目标文件必须真实存在（Gate 反查，虚构目标直接拦截）；ADD 条目须声明
> 所属模块与同类模式；每条带验证方式。数据库证据区分迁移脚本静态事实与目标库实际 schema。
> `baseline.repo_root` 记录基线调查的仓库根（条目目标按该根解析）；`related_acceptance[]` /
> `related_operations[]` 必须分别闭环到验收点与业务操作（悬空引用即拦截）。

## 1. 现有实现基线

| 对象 | 当前实现 | 证据（文件/符号） | 处理 |
|------|----------|-------------------|------|
| {入口，如 查询接口} | {如 XxxController#list} | `backend/.../XxxController.java` | 修改 |
| {核心逻辑} | {如 XxxService#page} | `backend/.../XxxService.java` | 复用并扩展 |
| {公共能力，如分页} | {如 PageQuery} | `common/.../PageQuery.java` | 直接复用 |
| {相关页面} | {如 views/xxx/index.vue} | `frontend/.../index.vue` | 修改 |

## 2. 预计代码变更

| 文件/符号 | ADD/MODIFY/DELETE | 设计依据（锚点/规则） | 验收点 |
|-----------|-------------------|----------------------|--------|
| `XxxController#list` | MODIFY | anchor: api-contracts §3.2.x（分文档 §5.3.x） | M-{xx}-F{y}-A{z} |
| `XxxService#page` | MODIFY | anchor: business-rules R{n} | M-{xx}-F{y}-A{z} |
| `XxxMapper.xml#selectPage` | MODIFY | anchor: data-model §2.2 {表名}（分文档 §2.3） | M-{xx}-F{y}-A{z} |
| `XxxServiceTest` | ADD | 验证 R{n} | M-{xx}-F{y}-A{z} |

## 3. 不变量

- 原有接口响应结构不得变化（本设计明确变更的除外）
- 原有调用方无需修改
- 未指定的既有行为保持不变
- 不升级依赖、不顺手重构无关代码
- {本 feature 特有的不变量，如"未指定查询条件时行为保持不变"}

## 4. 跨模块影响面索引（总分模式总文档使用；单体/分文档可省略）

| 模块 | 分文档 | 主要改动文件/符号 | ADD/MODIFY/DELETE | 验收点 |
|------|--------|-------------------|-------------------|--------|
| {M-01 模块名} | `M-01-{ModuleName}-详细设计.md` | 见各分文档实现交接 | {ADD/MODIFY/DELETE} | M-{xx}-F{y}-A{z} |

## 5. 变更历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0 | {YYYY-MM-DD} | 初稿 |
