---
name: 需求追溯-模板
version: "3.31.3"
description: >-
  需求追溯与覆盖率基线（v3.28.1 起从详设移出，独立成文）。
  trace-matrix 由 df_pipeline design --trace-doc 确定渲染，正本为 design.json acceptance[]。
---

# {FeatureName} 需求追溯与覆盖率基线

> 模板 ID：`需求追溯-模板`
> 模板版本：`{{template_version}}`（必须替换为本模板 frontmatter 的 version 后再过 Gate）
> 生成时间：{YYYY-MM-DD HH:mm:ss}

> **文档定位（v3.28.1）**：需求追溯与覆盖率基线从详设正文移出，本文件为独立正本。
> **机器契约正本**是 `.devflow/<feature>/design.json` `acceptance[]`；追溯矩阵由
> `python3 df_pipeline.py design --input .devflow/<feature>/design.json --doc docs/详细设计/<feature>-详细设计.md --trace-doc docs/详细设计/<feature>-需求追溯.md`
> 确定性渲染。零结果声明留在详设正文（设计本体完整性声明）。

<!-- anchor: acceptance-traceability -->

> **追溯源**：`docs/需求/<feature>-验收点.md`（P0 FROZEN）
> **追溯链**：P0 验收点 → 本矩阵 → P5 测试用例 → P6 测试报告
> **拆分粒度**：一个验收点 = 一个行为 = 一个测试用例

## 1. 验收点追溯矩阵

<!-- df:begin:trace-matrix -->
（由 design.json 自动生成 8 列追溯矩阵：ID|PRD 锚点|页面/任务|接口|数据|规则|测试用例|状态——与 criteria 冻结分母集合全等）
<!-- df:end:trace-matrix -->

> 列位契约（s2 Gate 逐列校验）：ID / PRD 锚点(#) / 页面任务 / 接口 / 数据 / 规则 / 测试用例(TC-) / 状态(COMPLETE)；
> 纯后端行为的页面/接口/数据列写 —（由 design.json `zero_results` 声明把关）。

## 2. 追溯覆盖率检查

| 检查项 | 命令 | 通过条件 |
|--------|------|----------|
| 验收点总数 | `grep -cE 'M-[0-9]{2}-F[0-9]{2}-A[0-9]{2}' <criteria>` | = P0 冻结数 |
| COMPLETE 状态 | `grep -c 'COMPLETE'` | = 验收点总数（100%） |
| 孤儿验收点 | `grep -cE 'PENDING\|WIP'` | = 0 |

```
设计覆盖率 = COMPLETE 原子验收点数 / 冻结原子验收点总数 = 100%
首轮代码准确率 = 首轮通过测试与 Review 的原子验收点数 / 冻结原子验收点总数 >= 80%
```

禁止使用篇幅、表格数量或接口数量替代验收点口径；编码开始后不得通过删除失败验收点提高准确率。

## 3. 验收点拆分规则

> **拆分粒度**：一个验收点 = 一个行为 = 一个测试用例

| 规则 | 说明 | 示例 |
|------|------|------|
| **单一行为** | 一个验收点只描述一个可观察行为 | ✅ "新增记录成功返回 ID"<br>❌ "新增和编辑记录" |
| **可验证** | 必须能用 API/UI 自动化验证 | ✅ "返回 200 状态码"<br>❌ "用户体验好" |
| **可失败** | 必须能明确判断成功或失败 | ✅ "字段为空时返回 400"<br>❌ "字段处理正确" |
| **ID 格式** | `M-{模块号}-F{功能号}-A{验收点号}` | `M-01-F01-A01` |

## 4. 存档位置

| 阶段 | 产物 | 存放位置 |
|------|------|----------|
| P0 澄清 | 原子验收点清单（冻结） | `docs/需求/<feature>-验收点.md` |
| P2 详设 | 本追溯矩阵 | 本文档 |

## 5. 变更历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0 | {YYYY-MM-DD} | 初稿 |
