---
name: 知识分享
version: "3.28.7"
description: 知识分享模板（P10；≥3 条可复用教训；结构化产物见 schema/sample，本模板为语义参考）
---

> **已结构化**：产物契约 `schemas/sharing.schema.json`、样例 `examples/structured/sharing.sample.json`；
> 填 `.devflow/<feature>/sharing.json` 后经
> `python3 scripts/df_pipeline.py sharing --input <json> --out docs/知识沉淀/<feature>-知识分享.md`
> 渲染（校验失败不渲染、不落盘）。Gate：`p10_feedback_gate.sh`。本模板为语义参考。

# 知识分享 - {title}

## 字段要点（正本见 schema）

| 字段 | 约束 |
|------|------|
| title / sharer / date | 分享标题、分享人、日期 |
| audience | 受众（可选） |
| lessons[] | ≥3 条可复用教训；每条含 topic + content（本次新发现的坑，禁止占位话术） |

## 渲染结构（产物章节正本）

- 可复用教训（≥3 条，逐条 topic + content）

## 通过条件

- 管线校验通过；`p10_feedback_gate.sh` 通过（教训条数 ≥3）。
