---
name: field-change
version: "3.23.0"
description: Use when a user explicitly asks to change one existing field, default, validation, or display label.
allowed-tools: [read, write, exec, glob, grep, task]
---

# /field-change - /small-change compatibility alias

字段改动现在属于通用 /small-change。加载 commands/small-change.md，使用 small-change.env 合同和 small-change-gate.sh；不要再创建旧 field-change.env。
