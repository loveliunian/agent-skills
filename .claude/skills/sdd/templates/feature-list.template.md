# Feature 清单(<设计名>)

> 使用说明(生成时删除本块):本文件与 feature-list.json 同源,先写 json 再派生本 md。
> 拆分标准:feature = 用户可感知、有明确边界、可独立交付;粒度 1~5 人日;每列不得留空。
> 验收要点必须可执行(能写出操作步骤得到明确结果),"正确""正常"这类词不算验收。
> 编号/边界/依赖/首批/验收/估时六项与 json 字段一一对应;估时列写 json 的 estimateDays 原值。
> 状态与 json 对应:待确认=草案;清单已确认(<日期>)=status=已确认,并同时回填 approvedBy/approvedAt。

来源详设:<路径> | 生成时间:<日期> | 状态:待清单确认 / 清单已确认(<日期>,确认人 <名字>)

架构形态:<单体 或 微服务——与 json 的 architecture、parse-report 的 architecture.style 三处一致>

## 清单

| 编号 | 名称 | 边界(做什么/不做什么) | 依赖 | 首批 | 估时(人日) | 验收要点 |
|---|---|---|---|---|---|---|
| F001 | <名称> | <做 X;不做 Y> | 无 | 是 | 3 | <可执行的验收描述> |
| F002 | <名称> | <做 X;不做 Y> | F001 | 否 | 2 | <可执行的验收描述> |
| F090 | <既有能力名> | <做 X;不做 Y> | 无 | 否(已交付) | 0 | <已交付,仅作依赖占位> |

> 上表 F090 演示 `done` 用法:开工前已交付/既有能力,json 里标 `"done": true` 且 `estimateDays` 填 0,md 的"首批"列填"否(已交付)"。
> 首批 feature 允许依赖它;除此之外首批的依赖必须都在首批内(R-D 首批闭环)。

## 拆分说明

- <拆分依据:按详设哪一章切的;粒度判断;横切 feature 有哪些>
- <首批选择理由:为何是这 2~4 个>
- <规则台账余量处置:rule-ledger 中非 assigned 条目(constitution/deferred/out-of-scope/pending)逐条说明去向,由用户裁决>

## 需要用户裁决的点

| 编号 | 问题 | 建议 |
|---|---|---|
| D1 | <拆分/排序上拿不准的点> | <建议方案> |

## 修订记录

| 日期 | 变更 | 确认人 |
|---|---|---|
| <日期> | 初稿生成 | — |
