# <F编号> <名称>

> 使用说明(生成时删除本块):本文件与 spec.json 同源,先写 json 再派生本 md。
> 状态与 json 对应:草案 / 已确认(<日期>) / 实现中 / 已交付(<日期>);md 与 json 两处同步,且已确认及以后必须回填 json 的 approvedBy/approvedAt。
> 接口多时契约另存 openapi.yaml(本目录),md 内放摘要表;契约外置后 json 的字段表可省,否则每条字段必须有 type 与 example。
> 规则文本禁止兜底词(黑名单以守门引擎 `BANNED` 表为准,含"等"收尾检查;“相等/等价”等实义词除外),写不清就登记 Q 并用 [Q00x] 三位编号占位。
> 每条规则必须回填 sourceRules(台账里分派到本 feature 的抽取规则编号),否则开工放行会报"分派后丢失"。
> 详设抽取件里有验收点时,本 feature 覆盖的验收点 ID 要写进 json 的 `coversAcceptances`,并在 §6 每条用例的 covers 列标明它验哪个验收点;两者必须一致(脚本反推:分派给本 feature 的规则所属验收点不得漏)。
> §3 的索引/唯一约束要同步登记进 json `tables[].indexes`(引擎扫「删了重插」×逻辑删除互斥靠它),md 与 json 两处都要有。

| 元信息 | 值 |
|---|---|
| 边界 | 做什么/不做什么 |
| 依赖 | F00x(前置 feature) |
| 来源 | 详设 §x |
| 状态 | 草案 / 已确认(日期) / 实现中 / 已交付(日期) |

## 1. 业务规则

| 编号 | 规则(主语+条件+结果) | 未决 | 来源规则(抽取件) |
|---|---|---|---|
| R1 | <完整可验收的规则句> | — | C01-R03 |
| R2 | <规则主体,含待澄清部分>[Q001] | Q001 | C01-R04、C03-R07 |

> `来源规则` 填 rule-ledger 中 disposition=assigned 且 feature=本 F编号 的条目编号;一条 spec 规则可对应多条来源规则。
> 由你推断新增的规则此列填“新增”,并必须出现在 §7 解释性决定清单里。

## 2. 接口契约

| 接口 | 方法+路径 | 用途 | 对应规则 |
|---|---|---|---|
| 创建 XX | POST /api/v1/xx | ... | R1,R2 |

<完整契约:openapi.yaml 或此处内嵌。每个接口必须给出:
<入参表:字段/类型/必填/示例/校验规则(确无入参时在 json 标 noRequest=true)>
<出参表:字段/类型/示例/说明>
<错误码:code/message/触发条件(json 字段名 when)>
以上三项在 json 中对应 requestFields / responseFields / errorCodes;挂了 openapi.yaml 时可降级为警告。

## 3. 数据模型

<每表一张;涉及既有表只写变更并注明影响>

表 <table_name>:<用途>

| 字段 | 类型 | 可空 | 默认 | 说明 |
|---|---|---|---|---|

索引:

| 索引名 | 列 | 唯一 | 用途 |
|---|---|---|---|

## 4. 非功能约束

维度取值限定 quality/performance/security/idempotency/compatibility/capacity/reliability/observability/operability/maintainability/scalability/compliance(与 json 枚举一致);归不进枚举的填 other 并写 subDimension,**不得因为无合适维度而丢弃要求**。有可度量目标值的写进要求列或 json 的 target。

| 维度 | 要求 | 来源 |
|---|---|---|
| 性能 | <如 接口 P95 < 500ms> | 详设 §x |
| 安全 | <鉴权/数据权限要求> | 详设 §x |
| 质量 | <如 核心包单测覆盖率≥80%> | 详设 §x |
| 幂等 | <写接口的幂等策略,无要求写"无明确要求"> | |
| 兼容 | <与既有功能的兼容约束> | |

## 5. 任务清单

<按实现顺序;每条一次可提交的工作量;第一个任务永远是数据层(若有)>

- 任务1 数据表变更脚本(含回滚说明)
- T2 后端接口 + 单测(覆盖 R1~Rn)
- T3 前端页面/组件
- T4 联调自验(对照验收标准逐条执行)

## 6. 验收标准

| 用例编号 | 对应规则/接口 | 覆盖详设验收点 | 步骤 | 期望结果 |
|---|---|---|---|---|
| A1 | R1 | <详设验收点 ID,无则 —> | <可执行的操作步骤> | <明确结果> |

## 7. 解释性决定清单(spec 确认时随附)

<列出所有非详设原文、由 AI 推断补齐的点;没有则写"无">

| 位置 | 我方理解 | 依据 |
|---|---|---|

## 8. 修订记录

| 日期 | 变更 | 确认人 |
|---|---|---|
